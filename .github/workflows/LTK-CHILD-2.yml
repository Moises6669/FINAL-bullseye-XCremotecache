name: LTK-child-2

on:
  workflow_dispatch:

jobs:
  call-workflow-test:
    uses: ./.github/workflows/LTK-principal.yml
  
  - name: testing
    if: ${{ env.VARIABLE_1 != " "}}
    run: |
      echo "todo bien"