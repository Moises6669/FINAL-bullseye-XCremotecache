name: ios-312
on:
  pull_request:
  workflow_dispatch:

jobs:
  build-macos:
      runs-on: macos
      steps: 
      - name: Checkout
        uses: actions/checkout@v2.4.0
      - name: werever
        run: echo hello
      - name: jira
        env:
          JIRA_TOKEN_ENV: ${{ secrets.JIRA_TOKEN }}
        run: |
          echo VARIABLE: $JIRA_TOKEN_ENV
          source ./scripts/jira.sh
