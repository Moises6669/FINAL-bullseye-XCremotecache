name: ios-312
on:
  pull_request:
  workflow_dispatch:

jobs:
  build-macos:
      runs-on: macos
      steps: 
      - name: jira
        run: | 
           NOW=$(date +"%D")
           summary="Test Execution result for en_us: ${NOW}  "
           description="tests passed"
           execution_data="$(jq --arg s "$summary" --arg d "$description" '.update .summary[] .set = $s | .fields.description = $d' source ./issue_template.json)"         
           curl -H "Authorization: Basic bW9pc2VzQGFsbC13aW4uc29mdHdhcmU6VFZEaXI5aTJWcTJrUmwzZGFzWE40N0I2" \
           -X PUT --data "${execution_data}" \
           -H "Content-Type: application/json" \
           https://moisesa.atlassian.net/rest/api/2/issue/TST13-178

